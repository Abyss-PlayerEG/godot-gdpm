# 安装类型检测

## 目标

检测 gdpm 的安装方式，用于显示信息、更新提示和错误报告。

## 安装类型

| 类型 | 标识 | 检测方式 |
|------|------|---------|
| pip | `pip` | `importlib.metadata.version('godot-gdpm')` |
| 二进制 | `binary` | `getattr(sys, 'frozen', False)` |
| 源码 | `source` | 以上都不匹配 |

## 检测逻辑

```python
def get_install_type() -> str:
    if getattr(sys, 'frozen', False):
        return 'binary'
    try:
        from importlib.metadata import version
        version('godot-gdpm')
        return 'pip'
    except Exception:
        return 'source'
```

## 使用场景

### 1. `gdpm -i` 显示安装类型

```
╭──────────────────────────── GDPM-Info ─────────────────────────────╮
│                                                                    │
│   gdpm v0.0.6                                                      │
│   Godot Dependency Package Manager                                 │
│   Install: pip                                                     │
│                                                                    │
│   GitHub: https://github.com/Abyss-PlayerEG/godot-gdpm            │
│                                                                    │
╰────────────────────────────────────────────────────────────────────╯
```

### 2. `gdpm update` 提示更新方式

```
# pip 安装
  Run 'pip install --upgrade godot-gdpm' to update.

# 二进制安装
  Download new version from GitHub Releases.

# 源码安装
  Run 'git pull && pip install -e .' to update.
```

### 3. 错误报告

```
gdpm v0.0.6 [pip]
Python 3.14.0
macOS arm64
```

## 实现步骤

- [ ] 添加 `utils/install.py` 检测安装类型
- [ ] `gdpm -i` 显示安装类型
- [ ] `gdpm update` 根据类型提示更新方式
- [ ] 错误报告包含安装类型

## 文件变更

| 文件 | 修改 |
|------|------|
| `utils/install.py` | 新增，检测逻辑 |
| `cli/app.py` | `-i` 显示安装类型 |
| `cli/update.py` | 更新提示 |
